name: rtm-education

services:
  db:
    image: postgres:17-alpine
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 10s
      timeout: 5s
      retries: 10
    networks:
      - internal

  backend:
    build:
      context: ./backend
    restart: unless-stopped
    environment:
      APP_ENV: ${APP_ENV:-production}
      APP_VERSION: ${APP_VERSION:-unknown}
      DATABASE_URL: postgresql+psycopg://${POSTGRES_USER}:${POSTGRES_PASSWORD}@db:5432/${POSTGRES_DB}
      BITRIX_PORTAL_HOST: ${BITRIX_PORTAL_HOST:-rtm-group.bitrix24.ru}
      KNOWLEDGE_SYNC_SECRET: ${KNOWLEDGE_SYNC_SECRET:-}
      PUBLIC_ORIGIN: ${PUBLIC_ORIGIN:-https://rtmgroupdocs.fvds.ru}
      GOOGLE_CLIENT_ID: ${GOOGLE_CLIENT_ID:-}
      GOOGLE_CLIENT_SECRET: ${GOOGLE_CLIENT_SECRET:-}
      GOOGLE_TOKEN_ENCRYPTION_KEY: ${GOOGLE_TOKEN_ENCRYPTION_KEY:-}
      DOCUMENT_RENDER_MEDIA_DIR: /app/data/document-renders
      YOUTUBE_CLIENT_ID: ${YOUTUBE_CLIENT_ID:-}
      YOUTUBE_CLIENT_SECRET: ${YOUTUBE_CLIENT_SECRET:-}
      VIDEO_TOKEN_ENCRYPTION_KEY: ${VIDEO_TOKEN_ENCRYPTION_KEY:-}
    depends_on:
      db:
        condition: service_healthy
      media-init:
        condition: service_completed_successfully
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8000/api/health', timeout=3)"]
      interval: 10s
      timeout: 5s
      retries: 10
    networks:
      - internal
    volumes:
      - video_media:/app/data/video-covers
      - /srv/rtm-education/document-renders:/app/data/document-renders

  media-init:
    build:
      context: ./backend
    user: "0:0"
    restart: "no"
    command: ["sh", "-c", "mkdir -p /app/data/video-covers /app/data/document-renders && chown -R app:app /app/data"]
    volumes:
      - video_media:/app/data/video-covers
      - /srv/rtm-education/document-renders:/app/data/document-renders

  frontend:
    build:
      context: ./frontend
      args:
        VITE_APP_ENV: ${APP_ENV:-production}
        VITE_APP_VERSION: ${APP_VERSION:-unknown}
        VITE_POSTHOG_ENABLED: ${VITE_POSTHOG_ENABLED:-false}
        VITE_POSTHOG_KEY: ${VITE_POSTHOG_KEY:-}
        VITE_POSTHOG_HOST: ${VITE_POSTHOG_HOST:-https://eu.i.posthog.com}
    restart: unless-stopped
    depends_on:
      backend:
        condition: service_healthy
    networks:
      - internal

  caddy:
    image: caddy:2.10-alpine
    restart: unless-stopped
    environment:
      DOMAIN: ${DOMAIN:-localhost}
    ports:
      - "80:80"
      - "443:443"
      - "443:443/udp"
    volumes:
      - ./deploy/Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy_data:/data
      - caddy_config:/config
    depends_on:
      frontend:
        condition: service_started
    networks:
      - internal

networks:
  internal:
    driver: bridge

volumes:
  postgres_data:
  video_media:
  caddy_data:
  caddy_config:

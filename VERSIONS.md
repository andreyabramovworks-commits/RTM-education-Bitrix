# Версии RTM Education

## v50.4.0 — визуальные редакторы и полная синхронизация назначений

- Связанные статьи загружают центральную сцену прямо в редакторе курса.
- Все тесты открываются в общем визуальном редакторе со сценой и правой панелью.
- Исправлены отображение доски разработчика, прокрутка карточек и шрифт/привязка кнопки завершения.
- Google Таблица синхронизирует назначения, проверяющих и редакторов статьи, лайт-теста и полного теста.

## v50.3.10 — связанные статьи и стабильная доска

Редактор связанной статьи теперь продолжает загрузку после отсутствующего черновика и использует живую сцену Базы знаний, а не создаёт пустое полотно. Карточка завершения автоматически удерживает подпись внутри кнопки. Страница материала передаёт явный ID в редакторы и назначения. Доска разработчика не монтируется дважды при одном переходе. Первая синхронизация Google Sheets безопасно идёт от PostgreSQL к таблице и не отправляет устаревший шаблон обратно на сервер.

## v50.3.8 — единая живая База знаний и безопасные сцены

Иконка прежней «Базы данных» открывает единое древовидное «Управление Базой знаний». Центральные статьи и тесты редактируются в PostgreSQL и подключаются к курсам как неизменяемые ссылки с независимым прогрессом курса. Добавлены понятные назначения людей и подразделений, раздельные ученики/проверяющие/редакторы, копирование назначений статьи в тесты по кнопке и локальная корректировка проверяющих курса. Исправлены пустые связанные статьи, пользовательский просмотр карточек, последовательная блокировка слайдов, размеры текста, лишняя рамка, безопасное копирование доски, восстановление ревизий и удаление лишних карточек «Завершить» при защите последней.

## v50.3.7 — видимый интерфейс живой Базы знаний

Модуль Базы знаний подключён к фактическому React-загрузчику. Пользователям доступны серверные карточки, администраторам и разработчику — отдельный раздел управления со статьями, тестами, назначениями, проверяющими, справочником и синхронизацией. Пустые тесты лайт и полный создаются только по явной кнопке администратора. Материалы Базы знаний подключаются к курсам как неизменяемые связанные блоки: централизованная правка сразу действует во всех курсах. В редактор статьи возвращена вставка нескольких карточек «Завершить», а их прохождение сохраняется раздельно.

## v50.3.6 — живая База знаний

PostgreSQL стал единым источником 193 карточек по всем строкам «Каталога документов» со ссылками на Google Docs. Добавлены эталонные статьи, шаблоны тестов лайт/полный, раздельные назначения, проверяющие и редакторы, серверный справочник, API двусторонней синхронизации Google Sheets, управление статьями в админке и добавление материалов Базы знаний в курс с локальной копией. Ручное сохранение Excalidraw теперь передаёт актуальный снимок доски и одинаково работает в проекте и внутри курса.

## v50.3.5 — видимая сохранённая статья

После вставки и повторного открытия редактор центрирует сохранённую сцену, а не восстанавливает камеру за пределами содержимого.

## v50.3.4 — стабильность доски и восстановление

Компактное окно результата теста, мобильный вход в Центр проверок, видимое назначение преподавателей, история и восстановление доски разработчика. Убрана регрессия перемещения фреймов, возвращён Ctrl+Z, а импортированная сцена сразу попадает в сохраняемый черновик статьи.

## v50.3.3 — Центр проверок

Единый центр ответов учеников, проверки и публикации материалов и назначений преподавателей. Добавлены наследование курса, блокировка проверки, отзыв ревизии, уведомления/задачи Bitrix24, мобильный вход и встроенное ролевое обучение. Также уплотнены карточки теста, модальное окно таймера и исправлена интеграция Excalidraw.

## v50.3.2 — восстановление медиа, явный старт и таймер

- старые и новые медиа получают свежую ссылку Bitrix перед показом;
- тест открывается только по кнопке, таймер виден и по истечении списывает попытку;
- внешний фрейм переносит содержимое всех вложенных фреймов.

Снимок публикуется по тегу `v50.3.2`; production автоматически получает эту версию из `main`.

## v50.3.1 — точная сцена, медиа и прохождение тестов

- Excalidraw стал источником текста, переносов и геометрии после публикации;
- медиа Bitrix.Диска обновляются через защищённый серверный поток и видны в редакторе, предпросмотре и тесте;
- исправлены выбор ответов и фото, вступительная карточка, Delete/Ctrl+Z, вложенные фреймы и стабильное перемешивание попытки.

Снимок публикуется по тегу `v50.3.1`; production автоматически получает эту версию из `main`.

## v50.3 — точное отображение макетов и тестов

- ручные переносы и размеры текста Excalidraw сохраняются при публикации и не заменяются строкой из панели настроек;
- изображения, аудио и видео отображаются в редакторе и мобильном предпросмотре тем же рендерером, что и у пользователя;
- выбранный ответ получает раздельные оранжевые рамки без галочки, а выбор изображения — только оранжевую рамку;
- подписи вариантов в новых шаблонах центрируются по ширине и высоте;
- восстановлены SVG-иконки и нормальная сетка карточек параметров теста.

Снимок публикуется по тегу `v50.3`; production автоматически получает эту версию из `main`.

## v50 — Excalidraw, тесты и навигация курса

- просмотр фреймов точно подгоняется по границам и не уменьшается ниже базового масштаба;
- редактор принимает Excalidraw clipboard JSON вместе с элементами, связями, текстом, изображениями и стилями;
- исправлены шрифты, переключатели подчёркивания и зачёркивания, а также карточка завершения;
- стрелочная навигация ограничена материалами одного курса и учитывает секции и обязательность;
- новый движок тестов поддерживает один или несколько ответов, соответствия, точный и свободный текст и результаты;
- запрос `tasks.task.add` кодирует вложенные поля по формату Bitrix24 и возвращает точную ошибку портала вместо безусловного 502.

## v49.2.6 — роли внутри мобильного меню

- селектор разработчика вставляется после штатной перерисовки мобильного меню и больше не удаляется старым обработчиком навигации.

## v49.2.5 — закрытие админки в роли пользователя

- предпросмотр роли пользователя теперь переключает приложение штатным способом и гарантированно закрывает активный административный контейнер.

## v49.2.4 — фактический вход в админку

- новый контроль доступа теперь сохраняет запрошенный административный режим после проверки совместимости со старым кодом, поэтому меняется не только подпись кнопки, но и активный интерфейс.

## v49.2.3 — предпросмотр ролей

- переключатель разработчика аккуратно выровнен на ПК, на телефоне перенесён внутрь мобильного меню под смену режима и остаётся доступным Bitrix24 ID 36 при просмотре любой роли.

## v49.2.2 — переключатель режима

- устранён конфликт со старой проверкой ролей, из-за которого кнопка перехода в админку оставалась отключённой у разработчика, администратора и редактора.

## v49.2.1 — настройка резервного копирования

- таймер защищённого листа разработчика запускается от системного пользователя `rtmdeploy`, которому доступен настроенный Google Drive remote.

## v49.2.0 — единый Excalidraw, роли, тесты и лист разработчика

- пользовательский просмотр статей переведён на тот же Excalidraw-рендерер, что и редактор; фреймы листаются последовательно и сохраняют исходные шрифты и геометрию;
- исправлены Rutube, Delete, переключаемые списки, меню и шрифты редактора, автосохранение названий и карточка «Завершить»;
- роли разделены на разработчика, администратора, редактора, преподавателя и ученика; роль разработчика закреплена за Bitrix24 ID 36;
- тесты получили новый интерфейс с переключателем на классический вид, свободные ответы, медиа и выдачу дополнительных попыток;
- во вкладке «Информация» добавлен закрытый лист разработчика с серверным автосохранением, историей и синхронизацией в Google Drive каждые 15 минут;
- добавлены миграции `20260720_0006` и `20260720_0007`.

Снимок публикуется по тегу `v49.2.0`; production автоматически получает эту версию из `main`.

Production-сервер автоматически разворачивает только последнюю проверенную
версию из ветки `main`. Стабильные снимки приложения публикуются отдельными
Git-тегами, поэтому их можно скачать или использовать для отката независимо от
дальнейшей разработки.

## v47.0.0 — стабильный интерфейс v46

- исходный визуальный интерфейс v46 сохранён без React-переписывания;
- данные проектов, курсов, статей и тестов синхронизируются с PostgreSQL;
- Excalidraw-сцены сохраняются на сервере и доступны другим сотрудникам;
- материалы, расположенные непосредственно в проекте, поддерживаются наравне с
  материалами внутри курса.

Скачать исходники можно на странице GitHub `Tags`, выбрав `v47.0.0` и архив
`zip` или `tar.gz`.

## v48.0.0 — React

- интерфейс монтируется React-приложением в основной документ без вложенного
  `iframe`;
- исходные DOM-структура и CSS v47 сохранены для визуального совпадения;
- серверное хранение Excalidraw и совместимость API v47 сохранены;
- `?version=47` открывает прежний iframe-вариант для оперативного сравнения;
- production-проверка выполнена внутри Bitrix24 с реальными данными.

Скачать исходники можно на странице GitHub `Tags`, выбрав `v48.0.0`. Сервер
автоматически использует v48, потому что этот код находится в ветке `main`.

## v48.1.2 — исправления обучения и Bitrix24

- полноэкранное окно использует защищённую серверную сессию и продолжает синхронизацию;
- восстановлены задачи Bitrix24, выбор файлов с ПК/Bitrix.Диска и сохранение названия статьи;
- добавлено завершение статьи с фиксацией прогресса и разблокировкой следующих материалов;
- панель редактора приведена к компактному виду, мобильные элементы управления закреплены;
- системные статусы перенесены в нижнюю часть профиля.

Снимок доступен по тегу `v48.1.3`; production автоматически получает эту версию из `main`.

## v49.0.0 — мобильное обучение, черновики и аналитика

- восстановлена вертикальная прокрутка пользовательских и административных страниц;
- полноэкранный режим открывает отдельную авторизованную вкладку на ПК и текущую вкладку на телефоне;
- мобильный просмотр подгоняет колонку по ширине, поддерживает Frames, автоматические колонки, стрелки и pinch-to-zoom;
- редактор получил рукописную панель, мобильный предпросмотр и устойчивое позиционирование медиа-слоёв;
- в новые статьи автоматически добавляется перемещаемая карточка завершения;
- серверные черновики автосохраняются отдельно и становятся видны ученикам только после публикации;
- карточка пользователя в аналитике показывает курсы, прогресс, тесты, активное время и историю действий.

Снимок публикуется по тегу `v49.0.0`; production автоматически получает эту версию из `main`.

## v49.1.0 — стабильная доставка и Excalidraw clipboard

- оболочка Bitrix24 и legacy-ресурсы больше не остаются на старой версии из кэша; интерфейс явно показывает `v49.1`;
- мобильные кнопки просмотра уменьшены, значок исходного масштаба заменён на рамку, а текст вписывается в границы карточек;
- полноэкранный режим всего приложения скрыт на телефоне, полноэкранный редактор статьи сохранён;
- панель редактора и штатные инструменты Excalidraw приведены к единому рукописному оформлению;
- карточка завершения стала компактной и цельной, а дубликаты старых автоматически созданных кнопок скрываются;
- вставка скопированных элементов с `excalidraw.com` переносит связи, группы, frames, изображения и выделяет вставленный макет.

Снимок публикуется по тегу `v49.1.0`; production автоматически получает эту версию из `main`.

## v50.1 — интерактивные тесты и точный просмотр Excalidraw

- пользовательский просмотр открывает выбранный frame в читаемом базовом масштабе, прокручивает длинные материалы по вертикали и разрешает горизонтальное смещение только после увеличения;
- редактор сохраняет шрифты и текстовые стили, поддерживает внутренний буфер RTM и перенос Excalidraw JSON вместе со связями, изображениями и оформлением;
- добавлен визуальный конструктор тестов с шестью типами вопросов, пятью шаблонами, автоматической проверкой и ручной проверкой свободных ответов;
- назначенный проверяющий принимает или возвращает каждый свободный ответ, а возвращённая работа исправляется в той же попытке;
- навигация ограничена порядком материалов одного курса, обязательные материалы продолжают блокировать следующий шаг, а кнопка завершения сразу открывает следующий доступный материал;
- роль разработчика доступна для выдачи только основному разработчику, при этом назначенные разработчики получают полный набор возможностей роли;
- исправлены создание пустых материалов, назначение Bitrix24-задач и соответствие кнопок панели редактора их назначению.

Снимок публикуется по тегу `v50.1`; production автоматически получает эту версию из `main`.

## v50.2 — цельный просмотр и исправленный цикл тестирования

- статьи и тесты отображаются одним синхронным Excalidraw-слоем: длинные материалы прокручиваются по вертикали без отставания интерактивных элементов, а горизонтальное смещение доступно только после увеличения;
- пользовательский тест повторяет дизайнерский макет, сохраняет интерактивность вариантов, изображений, медиа и свободных ответов и автоматически переносит ранее созданные тесты на новый шаблон;
- неуспешную и ожидающую проверки попытку можно пройти повторно: автоматические ответы очищаются, свободные подставляются, а проверяющему показывается последняя редакция с решениями по вопросам и по всей попытке;
- проверка свободных ответов перенесена из тесного модального окна на адаптивную страницу с мобильным режимом по одному вопросу;
- переходы и завершение материала учитывают порядок и обязательность внутри курса, а для материалов вне курса стрелочная навигация скрыта;
- вложенные фреймы автоматически получают правильное дерево принадлежности: элемент относится к ближайшему наименьшему фрейму, а перемещение внешнего фрейма охватывает всё вложенное содержимое;
- восстановлена полная undo/redo-история для штатных и RTM-действий, работают кнопки и `Ctrl+Z`/`Ctrl+Y`, исправлена повреждённая высота однострочных текстовых элементов;
- поиск, подразделение, период, сортировка и CSV-отчёт аналитики применяются к одной видимой выборке.

Снимок публикуется по тегу `v50.2`; production автоматически получает эту версию из `main`.

### v50.2.1

Исправлена загрузка визуальной сцены при повторном открытии, возобновлении и повторной попытке теста: холст теперь монтируется независимо от того, каким путём открыта попытка, и больше не остаётся пустым с нулевой высотой.

## Откат production

Откат выполняется осознанно: сначала нужный тег переносится в `main`, после
чего обычный автодеплой развернёт его на сервере.

```bash
git fetch origin --tags
git switch main
git pull --ff-only
git revert --no-commit v47.0.0..main
git commit -m "rollback: restore v47.0.0"
git push origin main
```

Перед откатом базы необходимо проверить совместимость миграций. Резервная
копия PostgreSQL создаётся отдельно от Git и исходников приложения.
